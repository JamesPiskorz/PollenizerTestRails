<h1>Search results</h1>
You searched for "<%= @query%>"
<ul>
<% @images_paginated.each do |image| %>
<li>
<%= image.title%>

<%= link_to image_tag(image.url_sq), {:action => 'show', :controller => 'images', :id => image.id}, :class => 'thumbnail' %>
</li>
<% end %>
</ul>

<%= will_paginate @images_paginated %>
